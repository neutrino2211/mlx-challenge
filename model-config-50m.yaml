# QRK 50M Model Config
# Safe for 16GB Mac, ~2-3 hours for 100M tokens

name: "QRK-50M"
overwrite: true

data:
  input_file: "train.jsonl"
  validation_file: "val.jsonl"  # Optional
  tokenizer_path: "tokenizer"   # Directory containing tokenizer.json
  preprocessing:
    max_context_size: 1024
    chunk_overlap: 0

  tokenizer:
    normal_vocab_size: 256
    special_tokens:
      pad: "<pad>"
      bos: "<s>"
      eos: "</s>"
      # ChatML tokens
      im_start: "<|im_start|>"
      im_end: "<|im_end|>"
      # Thought injection tokens
      knowledge_start: "<knowledge>"
      knowledge_end: "</knowledge>"
      k_res: "<k_res>"
      k_end: "<k_end>"

model:
  architecture: "llama"
  dimensions:
    hidden_size: 512
    intermediate_size: 1408  # ~2.75x hidden (SwiGLU)
    num_layers: 8
  attention:
    num_heads: 8
    num_kv_heads: null       # Full attention at this scale
    head_dim: null
    max_position_embeddings: 1024
  normalization:
    rms_norm_eps: 1.0e-6
  rope:
    theta: 10000
    traditional: false
    scaling: null
  misc:
    attention_bias: false
    mlp_bias: false
    tie_word_embeddings: true

training:
  epochs: 1
  hyperparameters:
    batch_size: 8
    learning_rate: 5.0e-4
    weight_decay: 0.1
    # iters: 12500  # Uncomment to use iterations instead of epochs

  scheduler:
    type: "cosine_with_warmup"
    min_lr_ratio: 0.1
    warmup_steps: 300

  optimization:
    optimizer: "adamw"

logging:
  log_dir: "logs"
  checkpoint_dir: "checkpoints"
  steps:
    logging_interval: 25
    checkpoint_interval: 2500
    validation_interval: 500
  metrics:
    log_loss: true
    log_perplexity: true
    log_tokens_per_second: true
    log_learning_rate: true
    log_tokens_processed: true

system:
  seed: 42
  device: "gpu"
